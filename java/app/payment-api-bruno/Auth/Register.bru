meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "firstname": "Alice",
    "lastname": "Smith",
    "email": "alice.smitasdasdh@example.com",
    "password": ""
  }
}

script:post-response {
  var jsonData = res.getBody();
  bru.setEnvVar("jwt_token", jsonData.token);
}
